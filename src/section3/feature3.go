package main

import "fmt"

func main() {
	//코드 서식을 지정해주는 유틸이 있다.
	//한 사람이 코딩한 것 같은 일관성을 유지 할 수 있다.
	//gofmt -h : 사용법
	//gofmt -w : 원본파일에 반영
	//go 의 checkStyle이 반영되어 있다고 생각 할 수 있음.

	for i := 0; i < 10; i++ {

	}
}
